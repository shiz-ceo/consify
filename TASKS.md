# docsivi: план работ и тасклист

Живой документ. Каждый пункт отмечается `[x]` сразу после выполнения и проверки. Если в ходе работы меняется решение или всплывает новая задача, она сначала записывается сюда.

Условные обозначения:
- `[-]` отложено по решению владельца, `[ ]` не сделано, `[~]` в работе, `[x]` сделано и проверено, `[!]` заблокировано или нужен ответ от владельца.
- **Проверка** в конце этапа это условие, при котором этап считается закрытым.
- **Риск** место, где мы не уверены, что поведение работает так, как написано в документации, и оно проверяется на практике.

---

## 0. Зафиксированные решения (источник истины)

| Тема | Решение |
|---|---|
| Что строим | Переиспользуемый каркас сайта документации, который запускается из коробки и кастомизируется без правки ядра |
| Название | `docsivi` |
| Схема | Ядро живёт в пакете `docsivi` (`packages/core`). Проект пользователя тонкий: `docs.config.ts`, `vite.config.ts`, `react-router.config.ts`, `content/`, `custom/` (папки `app/` нет, см. раздел про ветку `feature/react-router`) |
| Обновление | `bun update docsivi`. Файлы пользователя ядро не перезаписывает |
| Фреймворк | ~~Next.js~~ → **React Router (Vite)**, решение владельца 2026-09-24 (ветка `feature/react-router`; Next-версия осталась в `main`) |
| Основа | Fumadocs + MDX + Tailwind CSS + Shiki + Twoslash + KaTeX |
| Пакетный менеджер, рантайм, тесты | Bun (workspaces, `bun test`) |
| Валидация | Zod (последняя версия), версию сверить с актуальной документацией перед установкой |
| Тема | Стиль shadcn: чёрно-белая, токены на CSS-переменных, шрифт Geist, светлая и тёмная темы |
| Языки | Мультиязычность (i18n) обязательна с первого дня |
| Версии документации | Обязательны с первого дня (несколько версий, переключатель) |
| Twoslash | Включён, но обрабатывает только блоки с меткой `twoslash` |
| Публикация | Пока локально (`bun link` / workspace). Публикацию в реестр решаем позже |
| Деплой | Делает владелец сам. Каркас должен собираться и как Next-сервер, и как статика |
| Язык документации (контента) | Уточнить при подготовке шаблона. Язык общения в чате: русский |
| Скилл по документации | Придёт позже, сейчас не делаем. TypeDoc не используем |

### Что не делаем на первом этапе
- Подмену целых лейаутов (сайдбар, шапка). Пока кастомизируются только стили (токены, радиусы, шрифты), компоненты MDX и плагины.
- Демо-контент. Только минимум страниц, чтобы проверить работу.
- Адаптер под Vite, CLI `create-docsivi`, публикацию в npm, скилл.

### Известные риски (проверяются на прототипе)
1. **Версии × языки одновременно.** Fumadocs описывает версии (root folders) и i18n по отдельности. Их сочетание (`v1/guide.mdx` + `v1/guide.ru.mdx`) в документации не показано.
2. **Прятать `next.config` и `source.config.ts` за функциями ядра.** Инструменты Fumadocs читают эти файлы сами, поэтому нужно проверить, что реэкспорт и обёртка из пакета работают.
3. **Twoslash + собственные типы.** Компилятор резолвит типы из рабочей папки проекта, значит пакеты пользователя должны быть видны из `content/`.
5. **Node не снимает типы у `.ts` внутри `node_modules`.** `source.config.ts` и `next.config.ts` загружает сам Node (strip-only). Сейчас это работает, потому что workspace и `bun link` указывают на реальный путь вне `node_modules`. Для пакета, установленного из реестра, ядро придётся собирать в JS (`dist`). Записано в этап 17 (пункт 17.6).
4. **Автоподхват компонентов из `custom/components/`** в MDX без явной регистрации. Нужно проверить на уровне сборки (Next и Fumadocs MDX).

---

## Этап 0. Обсуждение и планирование

- [x] **0.1 Изучить историю от прежнего агента** (`docs-site-handoff.md`). Выделено то, что относится к общему каркасу (стек Fumadocs + MDX + Shiki + Twoslash, ориентир по виду на Liveblocks, идеи по компонентам и Twoslash), и отброшено то, что относится к `typed-mongoose` (TypeDoc, скиллы, специфика библиотеки).
- [x] **0.2 Собрать требования у владельца.** Проведены вопросы по продукту, стеку, кастомизации, базе, дизайну, языкам, хостингу и процессу. Ответы зафиксированы в таблице выше.
- [x] **0.3 Проверить возможности Fumadocs и Bun по актуальной документации.** Подтверждено: i18n, версии через `root` в `meta.json`, Twoslash через `fumadocs-twoslash`, математика через `remark-math` + `rehype-katex`, `llms.txt`, OG, статический экспорт поиска, workspaces и `bun test` в Bun.
- [x] **0.4 Согласовать схему работы** (ядро в пакете, тонкий проект пользователя, обновление через `bun update`, защита от поломок через Zod и semver).
- [x] **0.5 Написать этот тасклист** (`TASKS.md`) и получить одобрение начинать.

**Проверка этапа:** все решения записаны, владелец сказал «начинай».

---

## Этап 1. Скелет монорепозитория (Bun)

Цель: пустой, но собирающийся и проверяемый монорепозиторий, в котором дальше можно работать без переделок структуры.

- [x] **1.1 Проверить окружение.** Версия Bun (`bun --version`), Node.js (нужен для Next.js), наличие git. Записать версии сюда, в заметки этапа.
- [x] **1.2 Инициализировать git-репозиторий** в `docsivi/` и настроить `.gitignore` (`node_modules`, `.next`, `dist`, `.turbo`, `*.tsbuildinfo`, `.env*`). Первый коммит делается только по просьбе владельца.
- [x] **1.3 Корневой `package.json`.** `private: true`, `workspaces: ["packages/*", "apps/*"]`, общие скрипты (`dev`, `build`, `test`, `typecheck`, `lint`), поле `packageManager`/`engines` при необходимости.
- [x] **1.4 Создать пакет `packages/core`.** Имя `docsivi`, `type: module`, `exports` (пока заготовки: `.`, `./config`, `./plugins`, `./components`, `./theme.css`), `peerDependencies` на `react`, `react-dom`, `next` и т.д. (после выбора версий на этапе 3).
- [x] **1.5 Создать приложение `apps/starter`.** Зависимость `docsivi: "workspace:*"`. Пока пустая заглушка, которая позже станет шаблоном.
- [x] **1.6 Базовая конфигурация TypeScript.** Корневой `tsconfig.base.json` (strict, `moduleResolution: bundler`, `verbatimModuleSyntax` и т.д.), отдельные `tsconfig.json` в пакетах, скрипт `typecheck` на весь монорепозиторий.
- [x] **1.7 Линтинг и форматирование.** Выбрать инструмент (Biome или ESLint + Prettier), согласовать с владельцем, если возникнут сомнения. Настроить единые правила и скрипты `lint` / `format`.
- [x] **1.8 Настроить `bun test`.** Проверить, что тесты запускаются на уровне корня и пакета, добавить один дымовой тест-заглушку (`core` экспортирует версию) и убедиться, что он проходит. Изучить документацию Bun по тестам (моки, lifecycle, snapshot), как просил владелец.
- [x] **1.9 Корневой `README.md`** с описанием каркаса, структуры репозитория и запуска. Пока краткий, развернём на этапе 15.
- [x] **1.10 Файл `CHANGELOG.md`** и правила версионирования (semver) для пакета `docsivi`. Формат записей (Keep a Changelog).
- [x] **1.11 Проверить установку:** `bun install` проходит чисто, `bun run typecheck` и `bun test` зелёные.

**Проверка этапа:** свежий клон репозитория собирается командой `bun install`, `bun test` и `bun run typecheck` проходят.

---

## Этап 2. Ядро: конфигурация и валидация (Zod)

Цель: единый файл `docs.config.ts`, из которого строится всё остальное, с понятными ошибками.

- [x] **2.1 Установить Zod** и проверить актуальную версию и API по документации перед использованием (`z.object`, `z.strictObject`, `.default`, `.transform`, форматирование ошибок).
- [x] **2.2 Спроектировать схему конфигурации.** Разделы: `site` (название, описание, лого, URL, GitHub-репозиторий, ветка для «Edit on GitHub»), `i18n`, `versions`, `features`, `components`, `plugins`, `theme`, `nav`. Для каждого поля: тип, значение по умолчанию, описание.
- [x] **2.3 Реализовать `defineDocs(config)`.** Принимает типизированный объект, подсказки в редакторе через выведенные типы (`z.input` и `z.output`), возвращает нормализованный конфиг.
- [x] **2.4 Понятные сообщения об ошибках.** Свой форматтер поверх Zod: путь к полю, что ожидалось, что пришло, подсказка. Отдельно: сообщения об устаревших опциях («поле X переименовано в Y»).
- [x] **2.5 Механизм deprecated / миграций опций.** Таблица переименований с версией, в которой опция стала устаревшей. Предупреждение при запуске, а не ошибка, пока опция ещё поддерживается.
- [ ] **2.6 Загрузка конфигурации.** Найти и загрузить `docs.config.ts` из проекта пользователя (в Next.js, на этапе сборки и в рантайме). Проверить, что TypeScript-конфиг читается без отдельной компиляции.
- [x] **2.7 Тесты (`bun test`).** Валидные конфиги, невалидные конфиги (ошибки с точными сообщениями), значения по умолчанию, deprecated-опции, замороженность результата.
- [x] **2.8 Опубликовать публичные типы:** `DocsConfig`, `DocsConfigInput`, `DocsPlugin` и т.д. из `docsivi`.

**Проверка этапа:** пример конфига валидируется, неверный конфиг даёт читаемую ошибку, тесты проходят.

---

## Этап 3. Интеграция с Fumadocs и Next.js (тонкая оболочка)

Цель: `apps/starter` запускается и показывает страницу, при этом почти весь код лежит в пакете `docsivi`.

- [x] **3.1 Зафиксировать версии.** Актуальные версии `next`, `react`, `fumadocs-core`, `fumadocs-ui`, `fumadocs-mdx`, `tailwindcss`, проверенные по документации. Записать в заметки этапа.
- [x] **3.2 Собрать минимальный Fumadocs-сайт в `apps/starter` без обёрток.** Сначала «как в документации Fumadocs», убедиться, что стандартный вариант работает под Bun. Это контрольная точка перед абстракцией.
- [x] **3.3 Вынести источник контента.** Функция ядра, которая создаёт `source` (loader) по конфигу: базовый путь, i18n, страницы, дерево страниц.
- [~] **3.4 Обёртка `withDocsivi(nextConfig)`.** Оборачивает `next.config`, подключает Fumadocs MDX, подставляет нужные опции. Проверить **Риск 2**: пользователь может свести `next.config` к паре строк.
- [x] **3.5 Конфиг MDX (`source.config.ts`) из ядра.** Функция `createSourceConfig(docsConfig)`, которая возвращает `defineConfig(...)` для Fumadocs MDX. Проверить, что файл в проекте пользователя может быть реэкспортом.
- [x] **3.6 Страницы `app/`: минимальные реэкспорты.** Layout, страница документации (`[[...slug]]`), route для поиска, 404, `not-found`. Каждый файл в проекте пользователя должен состоять из 1–3 строк, а логика лежит в `docsivi`.
- [x] **3.7 Провайдеры и корневой layout** (`RootProvider`, тема, шрифты) в ядре.
- [x] **3.8 Провести проверку в браузере:** `bun dev`, открыть страницу, убедиться, что она рендерится, сайдбар и навигация работают.
- [x] **3.9 Проверить `bun run build`** и `bun start`. Записать время сборки.
- [~] **3.10 Тесты:** те, что можно проверить без браузера (генерация конфигов, пути, опции Fumadocs).

**Проверка этапа:** `apps/starter` работает в dev и build, при этом логика в `packages/core`.

---

## Этап 4. Тема и оформление (Tailwind, shadcn-стиль, Geist)

Цель: чёрно-белая аккуратная тема, полностью настраиваемая токенами.

- [x] **4.1 Подключить Tailwind CSS** нужной версии, проверить совместимость с Fumadocs UI. Записать версию.
- [x] **4.2 Система токенов.** CSS-переменные в стиле shadcn: `--background`, `--foreground`, `--card`, `--popover`, `--primary`, `--secondary`, `--muted`, `--accent`, `--border`, `--ring`, `--radius` и т.д., для светлой и тёмной тем. Значения нейтральные (чёрно-белые).
- [x] **4.3 Связать наши токены с переменными Fumadocs UI.** Чтобы стандартные компоненты Fumadocs подхватывали тему.
- [x] **4.4 Шрифт Geist** (Geist Sans и Geist Mono). Подключение через `next/font` или пакет `geist`, проверить лицензию использования и загрузку без внешних запросов, если возможно.
- [x] **4.5 Светлая и тёмная тема** с переключателем и учётом системной настройки, без «мигания» при загрузке.
- [x] **4.6 Файл `theme.css` ядра.** Экспортируется из пакета (`docsivi/theme.css`), подключается в проекте одной строкой.
- [x] **4.7 Кастомизация темы из конфига.** Поля `theme.radius`, `theme.colors`, `theme.fonts`. Конфиг превращается в CSS-переменные на этапе сборки. Плюс свой `custom/theme.css` поверх ядра (порядок слоёв: ядро → конфиг → custom).
- [~] **4.8 Слои и специфичность.** Использовать `@layer`, чтобы пользовательские стили всегда побеждали ядро без `!important`.
- [x] **4.9 Проверить контраст и доступность** основных элементов в обеих темах (WCAG AA для текста и интерактивных элементов).
- [~] **4.10 Документировать токены** как публичный контракт (список имён, которые мы не ломаем).

**Проверка этапа:** тема переключается, конфиг меняет цвета и радиусы, `custom/theme.css` переопределяет ядро.

---

## Этап 5. MDX-конвейер: код, Twoslash, математика

Цель: блоки кода уровня современной документации, с типами и проверкой примеров.

- [x] **5.1 Настроить Shiki (`rehypeCode`).** Темы `github-light` / `github-dark` (или согласованные), список предзагруженных языков.
- [x] **5.2 Трансформеры кода:** заголовок с именем файла, подсветка строк, подсветка слов, diff-маркеры (`[!code ++]` / `[!code --]`), фокус на строках, номера строк по желанию. Проверить каждый на демо-странице.
- [~] **5.3 Кнопка копирования и вкладки вариантов** (npm / pnpm / yarn / bun), включая запоминание выбора пользователя.
- [x] **5.4 Twoslash.** Установить и подключить `fumadocs-twoslash`, стили и `Popup`. Настроить так, чтобы обрабатывались **только** блоки с меткой `twoslash` (решение владельца).
- [x] **5.5 Опции компилятора для Twoslash** из конфига (`compilerOptions`, `paths`, дополнительные типы) плюс общий «пролог» для примеров. Поддержать директивы `// ^?`, `// @errors`, `// ---cut---`.
- [~] **5.6 Скорость и кэш Twoslash.** Проверить, есть ли встроенный кэш, измерить время сборки страницы с 5–10 блоками, задокументировать. **Риск 3:** резолв типов пользовательских пакетов из папки проекта.
- [x] **5.7 Математика.** `remark-math` + `rehype-katex` (KaTeX первым в списке rehype-плагинов), подключить CSS KaTeX, проверить inline и блочные формулы.
- [x] **5.8 Единая функция сборки MDX-конвейера** `createMdxOptions(config, plugins)` в ядре: собирает встроенные плагины и плагины пользователя в правильном порядке.
- [~] **5.9 Тесты:** порядок плагинов, включение и выключение возможностей через `features`, отсутствие Twoslash без метки.

**Проверка этапа:** страница-стенд показывает все виды блоков кода, hover-типы, ожидаемую ошибку компилятора и формулы.

---

## Этап 6. Библиотека MDX-компонентов

Цель: набор готовых компонентов, доступных в `.mdx` без импортов.

- [x] **6.1 Общий реестр компонентов** (`mdxComponents`) в ядре: единая точка, из которой берутся все встроенные компоненты и куда добавляются пользовательские.
- [x] **6.2 Callout / Banner** (типы: info, warning, error, tip и т.д.).
- [x] **6.3 Card и CardGrid** (с иконкой, заголовком, описанием, ссылкой).
- [x] **6.4 Tabs / Tab** (включая синхронизацию выбранной вкладки между страницами).
- [x] **6.5 Badge** (статусы: new, beta, deprecated и т.п.).
- [x] **6.6 Table** (обёртка для GFM-таблиц с единым видом).
- [x] **6.7 Steps** (пошаговые инструкции).
- [x] **6.8 Accordion.**
- [x] **6.9 FileTree.**
- [x] **6.10 TypeTable** (таблица свойств типа; проверить, что встроенное решение Fumadocs подходит).
- [x] **6.11 Mermaid** (диаграммы, в том числе тёмная тема; ленивая загрузка).
- [x] **6.12 Video** (встраивание видео).
- [~] **6.13 Единый вид и доступность.** Все компоненты используют токены темы, клавиатурная навигация, aria-атрибуты, поведение в тёмной теме.
- [x] **6.14 Публичные типы пропсов** и экспорт из `docsivi/components`.
- [~] **6.15 Тесты и стенд.** Страница со всеми компонентами (для проверки глазами), тесты пропсов, где это имеет смысл.

**Проверка этапа:** все компоненты доступны в MDX без импортов и выглядят согласованно в обеих темах.

---

## Этап 7. Мультиязычность (i18n)

- [x] **7.1 Конфиг языков:** `i18n.defaultLanguage`, `i18n.languages`, названия языков для переключателя. Валидация в Zod.
- [x] **7.2 `defineI18n` и `source` с i18n.** Структура контента (по суффиксам `page.ru.mdx` или папкам, выбрать вариант и записать причину).
- [x] **7.3 Маршрутизация по языку** (`/en/docs/...`, `/ru/docs/...`), middleware для определения языка, редирект с корня, поведение при отсутствии перевода страницы (запасной язык).
- [x] **7.4 Переключатель языка** в лейауте, сохранение текущей страницы при смене языка.
- [x] **7.5 Переводы интерфейса** (`defineTranslations` / `uiTranslations`): поиск, «Edit on GitHub», обратная связь, пагинация и т.д. Файлы переводов в ядре для en и ru, возможность дополнить в проекте.
- [x] **7.6 Локализованные поиск, `llms.txt`, OG и sitemap** (учёт языка).
- [x] **7.7 SEO:** `lang`, `hreflang`, альтернативные ссылки.
- [~] **7.8 Тесты:** валидация конфига, выбор языка, запасной язык.

**Проверка этапа:** сайт открывается на двух языках, переключатель сохраняет страницу, интерфейс переведён.

---

## Этап 8. Версионирование документации

- [x] **8.1 Структура контента с версиями** (`content/docs/v2/`, `content/docs/v1/`) и `meta.json` с `"root": "version"`.
- [x] **8.2 Конфиг `versions`** (список, версия по умолчанию, подписи, статус: latest / deprecated). Валидация.
- [x] **8.3 Переключатель версий** в лейауте. Проверить, что при смене версии открывается та же страница или индекс, если её нет.
- [x] **8.4 Баннер устаревшей версии** («вы смотрите документацию v1, актуальна v2»).
- [x] **8.5 Совмещение версий и языков (Риск 1).** Проверить `v1/guide.mdx` + `v1/guide.ru.mdx`, переключение версии при сохранении языка и наоборот. Если штатно не работает, найти обходной путь и записать в этом файле.
- [~] **8.6 Поиск, `llms.txt`, sitemap с учётом версий.**
- [x] **8.7 Тесты** логики выбора версии и запасных вариантов.

**Проверка этапа:** две версии × два языка работают вместе, переключатели корректно сохраняют состояние.

---

## Этап 9. Система плагинов (единый API расширений)

Цель: три вида плагинов в одном объекте, ядро объединяет их в правильном порядке.

- [x] **9.1 Проектирование контракта.** Тип `DocsPlugin`: `name`, необязательные `remark`, `rehype`, `shiki` (transformers, langs, themes), `components`, `routes`, `nav`, `search`, `hooks`. Описать порядок и правила слияния (кто выигрывает при конфликте).
- [x] **9.2 Zod-схема плагина.** Ранняя и понятная ошибка при неверном плагине (имя, типы полей, дубликаты имён).
- [x] **9.3 Слияние MDX-плагинов** (remark/rehype) в конвейер с сохранением порядка.
- [x] **9.4 Слияние плагинов Shiki** (transformers, языки) с уже включёнными нашими.
- [~] **9.5 Плагины уровня сайта:** дополнительные разделы, маршруты, пункты навигации, источники поиска. Решить, где в Next.js это регистрируется, чтобы работало и без правки `app/`.
- [ ] **9.6 Жизненный цикл и хуки** (например, `onBuildStart`, `transformPage`). Согласовать с владельцем состав, не выдумывать лишнего.
- [~] **9.7 Встроенные плагины** оформить тем же API (Twoslash, Math, Mermaid). Так проверяется, что API достаточно выразителен.
- [x] **9.8 Примеры пользовательских плагинов** (кастомный remark, кастомный transformer Shiki, простой раздел сайта) в `apps/starter/custom/plugins/`.
- [ ] **9.9 Документация плагинов** (что можно, чего нельзя, гарантии совместимости).
- [~] **9.10 Тесты:** порядок, конфликты, валидация, отключение встроенных плагинов.

**Проверка этапа:** три примера пользовательских плагинов работают, встроенные плагины используют тот же API.

---

## Этап 10. Пользовательские компоненты и стили (`custom/`)

- [x] **10.1 Автоподхват компонентов** из `custom/components/` (**Риск 4**). Выбрать механизм: генерация реестра на этапе сборки, glob-импорты или явный реестр с автогенерацией. Записать выбранный подход и причину.
- [x] **10.2 Правила именования** (имя файла → имя компонента в MDX), обработка коллизий с встроенными.
- [x] **10.3 Переопределение встроенных компонентов** (`components: { Callout: MyCallout }`), гарантированный приоритет пользовательских.
- [x] **10.4 Подключение `custom/theme.css`** и порядок слоёв.
- [x] **10.5 Горячая перезагрузка** при изменении файлов в `custom/` в режиме `bun dev`.
- [~] **10.6 Типы для пользовательских компонентов** (чтобы в MDX работали подсказки, если это реализуемо, иначе записать ограничение).
- [x] **10.7 Пример:** свой компонент, свой стиль, переопределение встроенного в `apps/starter/custom/`.
- [~] **10.8 Тесты и проверка в браузере.**

**Проверка этапа:** положил файл в `custom/components/`, использовал его в `.mdx` без импорта и без правок ядра.

---

## Этап 11. Функции сайта

- [~] **11.1 Поиск.** Встроенный поиск Fumadocs (локальный индекс). Поддержка i18n, версий и статического экспорта (`staticGET`).
- [x] **11.2 Оглавление справа** (TOC), хлебные крошки, prev/next.
- [x] **11.3 «Edit on GitHub»** (из `site.github`, ветки и пути к файлам с учётом языка и версии).
- [-] **11.4 Виджет обратной связи.** Согласовать с владельцем, куда сохраняются ответы (по умолчанию интерфейс и хук-обработчик, который пользователь подключает сам). Не придумывать бэкенд без решения.
- [x] **11.5 `llms.txt` и `llms-full.txt`** (через `remark-llms` или встроенные средства Fumadocs), с учётом языков и версий.
- [x] **11.6 OG-картинки** (генерация на страницу, для статического экспорта: заранее).
- [x] **11.7 Sitemap, robots, canonical.**
- [~] **11.8 Флаги `features`.** Каждая функция включается и выключается из конфига, отключённая функция не попадает в сборку.
- [~] **11.9 Тесты и проверка в браузере.**

**Проверка этапа:** все функции включены по умолчанию, работают в обеих режимах сборки, отключаются флагами.

---

## Этап 12. Главная страница и шаблонные страницы

- [~] **12.1 Главная страница:** hero (заголовок, подзаголовок, кнопки), сетка карточек возможностей. Всё берётся из конфига или из файла контента, с переводами.
- [~] **12.2 Страница 404** (локализованная, со ссылкой на главную).
- [~] **12.3 Шапка и подвал** (лого, ссылки, переключатели языка, версии и темы, GitHub).
- [~] **12.4 Минимальный набор страниц-заглушек** в шаблоне (без демо-контента, только чтобы сайт показывал структуру): «Getting started», по одной странице на язык и версию.
- [~] **12.5 Адаптивность** (телефон, планшет, десктоп): проверка в браузере.

**Проверка этапа:** главная и структура сайта выглядят завершённо на всех размерах экрана.

---

## Этап 13. Прототип и проверка в браузере

- [~] **13.1 Страница-стенд возможностей** (только в `apps/starter`): все компоненты, блоки кода, Twoslash-примеры (`// ^?`, `// @errors`, `// ---cut---`), формулы, Mermaid.
- [~] **13.2 Запуск `bun dev` и осмотр в браузере** (светлая и тёмная темы, два языка, две версии, переключатели, поиск).
- [ ] **13.3 Мобильный вид** (эмуляция телефона и планшета).
- [~] **13.4 Продакшен-сборка** (`bun run build`), проверка ошибок Twoslash, битых ссылок, размера и времени сборки. Записать цифры.
- [ ] **13.5 Показать результат владельцу** (скриншоты или живой запуск) и собрать замечания в этот файл новым разделом.
- [ ] **13.6 Исправить критичные замечания**, остальные внести в тасклист.

**Проверка этапа:** владелец видит рабочий прототип и принимает или формулирует правки.

---

## Этап 14. Режимы сборки и деплоя

Деплой делает владелец, поэтому нужно чёткое описание.

- [x] **14.1 Режим Next-сервера** (`next build` + `next start`, в том числе standalone-вывод для Docker).
- [x] **14.2 Режим статического экспорта** (`output: 'export'`), проверка поиска, OG, i18n, версий, `llms.txt`.
- [x] **14.3 Переключатель режима в конфиге** (`deploy.mode`) и понятные ошибки, если включённая функция несовместима с режимом.
- [ ] **14.4 Пример Dockerfile** (Bun) для режима сервера.
- [ ] **14.5 Инструкция по деплою** (что и куда копировать, переменные окружения, базовый путь `basePath`).
- [~] **14.6 Проверка в обоих режимах** (сборка, запуск, основные страницы).

**Проверка этапа:** сайт собирается и открывается в обоих режимах.

---

## Этап 15. Тесты, качество, CI

- [ ] **15.1 Единый прогон:** `bun test`, `typecheck`, `lint` одной командой (`bun run check`).
- [ ] **15.2 Тесты конфигурации, плагинов, i18n и версий** доведены до разумного покрытия.
- [ ] **15.3 Проверка структуры пакета** (`exports`, типы, отсутствие лишних файлов при `bun pm pack --dry-run` или аналогичной проверке).
- [~] **15.4 Проверка «как у пользователя».** Скрипт или тест, который копирует шаблон во временную папку, ставит ядро как внешнюю зависимость и собирает сайт. Так ловим проблемы, которых не видно внутри монорепозитория.
- [ ] **15.5 CI** (по запросу владельца, платформа уточняется): `install`, `check`, `build`, проверка битых ссылок.
- [ ] **15.6 Политика версий** (semver, CHANGELOG, что считаем публичным контрактом).

**Проверка этапа:** одна команда подтверждает, что всё в порядке, включая сборку «как у пользователя».

---

## Этап 16. Документация самого каркаса

- [ ] **16.1 README репозитория** (что это, быстрый старт, структура).
- [ ] **16.2 Руководство «Создать новый сайт из шаблона»** (копирование `apps/starter`, `bun link`, первый запуск).
- [ ] **16.3 Руководство по кастомизации:** тема, компоненты, плагины, конфиг (с примерами кода).
- [ ] **16.4 Руководство по обновлению** (`bun update docsivi`, что смотреть в CHANGELOG, что делать при предупреждениях о deprecated).
- [ ] **16.5 Справочник конфигурации** (все поля, значения по умолчанию, примеры).
- [ ] **16.6 Справочник плагинов и токенов темы.**
- [ ] **16.7 Документация как сайт на самом `docsivi`** (по решению владельца, «съесть собственную еду»).

**Проверка этапа:** человек, впервые видящий репозиторий, создаёт свой сайт по руководству без вопросов.

---

## Этап 17. Распространение

- [ ] **17.6 Собрать ядро в JS (`dist` + `.d.ts`) перед публикацией.** Node (загрузчик `source.config.ts` и `next.config.ts`) не снимает типы у файлов в `node_modules` (`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`), поэтому для пакета из реестра нужен скомпилированный вывод хотя бы для входов `docsivi`, `docsivi/source-config`, `docsivi/next-config`. Сейчас работает только потому, что workspace и `bun link` резолвятся во внешний путь. Выбрать инструмент сборки (`tsc` 7 или `bun build`), обновить `exports` (условия `types`/`import`), проверить установкой из `bun pm pack` во временный проект.

- [x] **17.1 Схема локального подключения:** `bun link` в `packages/core`, `bun link docsivi` в проекте, проверка на реальном втором проекте вне монорепозитория.
- [ ] **17.2 Проверка обновления:** поднять версию ядра, применить в проекте-потребителе, убедиться, что `content/` и `custom/` не затронуты.
- [ ] **17.3 Решение по публикации** (npm, приватный реестр, GitHub Packages), подготовка пакета (`files`, `exports`, `types`, `license`), пробная публикация по разрешению владельца.
- [ ] **17.4 CLI `create-docsivi`** (`bun create docsivi my-docs`): копирует шаблон, ставит зависимости, задаёт вопросы (название, языки).
- [ ] **17.5 Команда `docsivi upgrade`** (по необходимости): обновление тонких файлов `app/` при изменениях в оболочке.

---

## Этап 18. Позже (отложено по решению владельца)

- [ ] **18.1 Скилл по документации** (владелец пришлёт исходник, адаптируем под каркас).
- [ ] **18.2 Адаптер под Vite** (React Router / TanStack Start), общая логика ядра остаётся.
- [ ] **18.3 Подмена лейаутов** (сайдбар, шапка), если понадобится.
- [ ] **18.4 Демо-контент и шаблоны страниц.**
- [ ] **18.5 Расширенные интеграции** (аналитика, OpenAPI и т.д.) по запросам.

---

## Открытые вопросы к владельцу

Пока пусто. Новые вопросы записываются сюда и снимаются после ответа.

## Журнал решений и заметок

- 2026-09-23: тасклист создан (шаг 0). Все решения из таблицы в начале подтверждены владельцем.
- 2026-09-23: окружение этапа 1: Bun 1.4.0, Node 24.14.0, git 2.50.1. Установлены `typescript` 7.0.2 (нативный компилятор) и `@types/bun` 1.4.2. `bun install`, `bun run typecheck` и `bun test` проходят.
- 2026-09-23: git-репозиторий инициализирован, коммитов пока нет (по просьбе владельца).
- 2026-09-23: линтер и форматтер: Biome 2.5.14 (выбран по рекомендации, владелец сказал «делай»). Команды: `bun run lint`, `bun run format`, `bun run check` (lint + typecheck + test).
- 2026-09-23: заметки по `bun test` (документация Bun): хуки `beforeEach`/`afterEach`/`onTestFinished` из `bun:test`; моки через `mock()`/`jest.fn()`, `spyOn`, `mock.module()`; **`mock.restore()` и `mock.clearAllMocks()` не сбрасывают override от `mock.module()`**, поэтому в `afterEach` их вызываем, а модульные моки убираем явно; снимки через `toMatchSnapshot()`.
- 2026-09-23: Zod 4.6.5 (закреплена точная версия). Заметки: вложенные значения по умолчанию задаём через `.prefault({})` (в Zod 4 `.default()` принимает значение выходного типа, а не входного); ошибки форматируем через `z.prettifyError`; сообщение об ошибке ключа в `z.record` Zod заменяет на общее «Invalid key in record», поэтому имена компонентов проверяем в `superRefine`.
- 2026-09-23: найдена и исправлена ошибка тестом: `structuredClone` падает на функциях (React-компоненты в `components`), поэтому `applyDeprecations` копирует только ветки вдоль изменяемого пути.
- 2026-09-23: этап 2: реализованы схема (`site`, `i18n`, `versions`, `features`, `theme`, `components`, `plugins` (пока минимальная форма), `nav`), `defineDocs`/`parseDocsConfig`, `DocsConfigError`, таблица устаревших опций (пока пустая), 21 тест. Осталась задача 2.6 (загрузка `docs.config.ts` из проекта), она зависит от Next.js и делается на этапе 3.
- 2026-09-23: **этап 3.** Версии: next 16.3.6, react 19.3.0, fumadocs-core/ui 16.15.13, fumadocs-mdx 15.4.3, tailwindcss 4.3.3, typescript 7.0.2. Заметка: в Fumadocs 16 коллекция задаётся макросом `defineDocs` из `fumadocs-mdx/macro` прямо в коде (нет обязательного `source.config.ts`), макрос **нельзя реэкспортировать**, поэтому он вызывается в `lib/docsivi.ts` проекта, а всё остальное лежит в ядре.
- 2026-09-23: **Риск 2 проверен.** Работает: `next.config.ts` = `withDocsivi()`, `source.config.ts` = `createSourceConfig(config)`, страницы `app/` по 1–8 строк. Не работает: `export const config` в `proxy.ts` (Next требует литерал), поэтому matcher записан в проекте. Для проекта нужны прямые зависимости `fumadocs-mdx` (макрос), `tailwindcss`, `@tailwindcss/postcss` и `postcss.config.mjs`.
- 2026-09-23: Node загружает `source.config.ts` в режиме strip-only: нужны явные расширения `.ts` в относительных импортах и запрещены параметры-свойства конструктора. В `tsconfig.base.json` включены `allowImportingTsExtensions` и `erasableSyntaxOnly`.
- 2026-09-23: **этап 4.** Токены shadcn (нейтральная палитра, oklch) в `@layer base`, связь с Fumadocs через `fumadocs-ui/css/shadcn.css`, Geist через пакет `geist` 1.7.2, переопределения из `theme` в конфиге рендерятся `<style id="docsivi-theme">` (значения валидируются: без `{ } < > ;`). Пользовательский CSS проект подключает сам после `docsivi/theme.css`, поэтому поле `theme.css` из схемы убрано. Проверено в браузере: Geist, радиус из конфига.
- 2026-09-23: **этап 5.** Shiki (`github-light`/`github-dark`), transformers Fumadocs, Twoslash только для блоков с меткой `twoslash` (проверено: `// ^?` и `// @errors` работают), файловый кэш Twoslash включён по умолчанию, KaTeX (стили подключены в `theme.css` всегда), Mermaid через ```mermaid. Опции коллекции `mdxOptions` в макросе **заменяют** дефолты Fumadocs, поэтому глобальные опции идут через `source.config.ts`. Сборка стенда: около 10 с.
- 2026-09-23: **этап 6/7/9.** Реестр компонентов (`getMDXComponents`: встроенные, плагины, конфиг, переопределения), новые Badge/Video/Mermaid, i18n en+ru (маршруты `/[lang]/...`, переключатель, redirect через proxy), контракт плагинов `DocsPlugin` (`remark`, `rehype`, `shiki`, `components`) и `definePlugin`.
- 2026-09-23: **этап 8, Риск 1 снят.** Версии (`content/docs/v1|v2` с `meta.json` `{"root": "version"}`) и языки (`*.ru.mdx`) работают вместе: переключатель версий сохраняет страницу (нет страницы в целевой версии → индекс), смена языка сохраняет версию, отсутствующий перевод падает на английский. `/en/docs` без версии редиректит на версию по умолчанию (для статики генерируются пустые slug), у `deprecated`-версии показывается локализованный баннер со ссылкой на актуальную. Сообщения docsivi (`documentation`, `deprecatedVersion`, `goToLatest`, `editOnGithub`) есть для en и ru, переопределяются через `i18n.messages`.
- 2026-09-23: **этап 10, Риск 4 снят.** Автоподхват: `custom/components/Foo.tsx` (PascalCase, default export) → `<Foo />` в MDX без импорта. Механизм: `withDocsivi()` генерирует `.docsivi/components.generated.ts` (в dev следит за папкой через `fs.watch`, проверено: добавление и удаление файла без перезапуска). Приоритет: встроенные → плагины → `config.components` → найденные в `custom/` → переопределения страницы. Проверены пример-компонент, `custom/theme.css` (перебивает ядро) и пример rehype-плагина (`custom/plugins/external-links.ts`). Важно: `next.config` перечитывается только при перезапуске dev-сервера.
- 2026-09-23: этап 11 (часть): флаги `toc`, `breadcrumbs`, `pagination` управляют `DocsPage`, «Edit on GitHub» строит ссылку из `site.github` с учётом языка и версии (проверено в HTML: `blob/main/content/docs/v2/index.ru.mdx`). Осталось: `llms.txt`/`llms-full.txt` (нужен `includeProcessedMarkdown` и хелпер `llms()` из Fumadocs, есть примеры в `examples/next`), OG-картинки, обратная связь, sitemap/robots, локализация поиска.
- 2026-09-23: состояние проверок: `bun run check` (Biome + typecheck + 36 тестов) зелёный, `bun run build` в `apps/starter` проходит (около 10 с), в браузере проверены: тема и Geist, все компоненты стенда, Twoslash, формулы, Mermaid, версии × языки, баннер, редирект, автоподхват, `custom/theme.css`, плагин.
- 2026-09-24: виджет обратной связи (11.4) отложен владельцем («пока не надо»). Флаг `features.feedback` убран из схемы, вернём вместе с задачей.
- 2026-09-24: **этап 11.** `llms.txt` и `llms-full.txt` (маршруты `/{lang}/llms.txt` лежат под `[lang]`, потому что прокси редиректит пути без языка; в проекте нужен `docs: { postprocess: { includeProcessedMarkdown: true } }` в `defineDocs()`), OG-картинки (`/{lang}/og/{slug}/image.png`, через `fumadocs-ui/og`, `takumi-js` не нужен), `sitemap.xml` и `robots.txt` (нужен `site.url`, иначе sitemap пуст), canonical и hreflang в метаданных страницы. Известное ограничение: MDX-компоненты в `llms-full.txt` остаются JSX-разметкой (у `remark-llms` есть опция `output`, сделаем позже). Обратная связь отложена владельцем.
- 2026-09-24: **этап 14.** Режим задаётся `deploy.mode` (`server` | `static`) и `deploy.basePath`. `static` = `output: "export"` + `images.unoptimized`. Прокси в статике не работает, поэтому `/` и `/{lang}/docs` редиректят через `<meta http-equiv="refresh">` (React 19 поднимает его в `<head>`; настоящий `redirect()` в экспорте даёт только JS-редирект). Поиск в статике: `staticGET` + клиентский диалог `StaticSearchDialog` (индекс скачивается один раз, проверено в браузере). Для экспорта нужны `generateStaticParams` в `[lang]` и `export const dynamic = "force-static"` в `sitemap.ts`/`robots.ts` (в шаблоне уже есть). `withDocsivi` теперь принимает конфиг: `withDocsivi(docsConfig, nextConfig?)`. Размер статики 11 МБ на 8 страниц, стоит проверить, что в нём лишнего (Mermaid и др.).
- 2026-09-24: **этап 17.1 проверен на реальном внешнем проекте.** Копия `apps/starter` вне монорепозитория плюс `bun link` собирается. Для этого потребовалось: (1) самодостаточный `tsconfig.json` шаблона (раньше наследовал корневой), (2) `typescript` в зависимостях шаблона, (3) `withDocsivi` сам расширяет `turbopack.root` до общего предка, когда ядро подключено через `link` (Turbopack иначе не компилирует файлы вне корня; для этого в `exports` ядра добавлен `./package.json`). Ограничение: при `link` из далёкого места общий предок может оказаться очень высоким (вплоть до `/`), поиск файлов замедлится. При обычной установке из реестра это не применяется.
- 2026-09-24: `.claude/launch.json` содержит две конфигурации предпросмотра: `starter` (dev, порт 3100) и `starter-static` (собранный `apps/starter/out`, порт 3200).
- 2026-09-24: **демо `apps/demo` (вымышленная библиотека «Lattice»).** Запуск: `bun run demo` (порт 3300). Содержимое: главная (hero и карточки из `home` в конфиге, en/ru), версии v2 (актуальная, около 25 страниц) и v1 (устаревшая, 3 страницы), языки en и ru (для v2 переведены ключевые страницы, остальные показываются на английском), разделы и вложенные категории с иконками и разделителями (`Learn` > `Core concepts`, `Guides` > `Advanced`, `API reference`, `Writing docs`), страницы про все возможности системы (компоненты, блоки кода, Twoslash, формулы, Mermaid, кастомные компоненты, плагины, темы, языки и версии). Кастомизация в демо: компоненты `ApiEndpoint`, `Since`, `Counter` (клиентский, интерактивный), `Release`; плагины `github-alerts` (цитаты `> [!NOTE]` → Callout) и `external-links`; свой `custom/theme.css`. Twoslash проверяет все примеры против настоящего локального пакета `lattice` (`apps/demo/lattice`, подключён как workspace-зависимость), сборка падает при некомпилируемом примере. Сборка: 142 страницы за 15 с.
- 2026-09-24: в ядро добавлено: конфигурируемая главная (`home` по языкам, `/docs` ведёт на версию по умолчанию), иконки Lucide в `meta.json`/frontmatter (`icon: Rocket`; в Lucide 1.x `Home` → `House`, `History` нет, взяли `ScrollText`; неизвестное имя даёт предупреждение при сборке).
- 2026-09-24: находки демо. (1) **Компонент в заголовке ломает сборку** (`## Title <Since />`): заголовки попадают в оглавление, а оно строится вне области видимости MDX-компонентов. Решение: не ставить компоненты в заголовки (написано на странице «Custom components»). (2) В Twoslash `paths` не работает (виртуальная папка), надёжнее подключить библиотеку как обычную зависимость. (3) Типы Node для Twoslash: `compilerOptions.types: ["node"]`. (4) В MDX нельзя писать ``` сразу после `<Tab ...>` на той же строке: нужны пустые строки.
- 2026-09-24: **известное предупреждение только в dev:** при переключении языка в консоли появляется `Encountered a script tag while rendering React component` (стек указывает на `RootProvider` в `root-layout.tsx`). Воспроизведено в `bun run demo`, в production-сборке (`next start`) того же сценария ошибки **нет**. Причина не в docsivi: `[lang]` стоит в корне, при смене языка Next пересоздаёт корневой layout, `next-themes` (внутри `RootProvider` Fumadocs, версия 0.4.6) заново монтируется на клиенте и снова рисует `<script>` для темы, а React 19 в dev на это ругается. Поведение и внешний вид страниц не страдают. Решение отложено (кандидаты: обновить `next-themes` при выходе исправления, либо отдельный `ThemeProvider` без скрипта в клиентском монтировании); при желании владельца берём в работу. В `.claude/launch.json` добавлена конфигурация `demo-prod` (порт 3400, `next start` после `next build`).

---

## Ветка `feature/react-router`: переход с Next.js на React Router + Vite (2026-09-24)

Решение владельца: убрать Next.js полностью, всё описывать в конфигах роутера и «забыть». `main` содержит Next-версию (коммит `6a7cdc8`), эта ветка её заменяет. Пункты этапов 3, 11, 14, 17 выше, где упомянуты Next-специфичные вещи (`next.config`, `proxy.ts`, `generateStaticParams`, `turbopack.root`, `app/`), **заменены** описанным ниже.

### Новая архитектура
- **Проект пользователя = только конфиги:** `docs.config.ts`, `vite.config.ts` (`plugins: [docsivi(config)]`), `react-router.config.ts` (`defineRouterConfig(config)`), `content/`, `custom/`. Папки `app/` нет.
- **Генерируется в `.docsivi/`** (gitignored, создаётся при запуске): `instance.ts` (макрос `defineDocs` из Fumadocs и `createDocsivi`), `app/root.tsx`, `app/routes.ts` (по одной строке-реэкспорту из пакета). React Router получает `appDirectory: ".docsivi/app"`.
- **Маршруты лежат в пакете** (`packages/core/src/react-router/routes/*`): `/`, `/:lang`, `/:lang/docs/*`, `/:lang/llms.txt`, `/:lang/llms-full.txt`, `/:lang/og/*`, `/api/search`, `/sitemap.xml`, `/robots.txt`, `*` (404). Они берут экземпляр через алиас `docsivi:instance`.
- **Vite-плагин `docsivi(config)`** собирает `fumadocsMdx` (MDX-опции идут через `globalOptions`, `source.config.ts` не нужен), `@tailwindcss/vite`, `reactRouter()` и связку (алиас, `ssr.noExternal`, `fs.allow`).
- **CLI `docsivi dev|build|start|typegen`** запускает React Router CLI ядра, поэтому проект не зависит от `@react-router/dev` (в монорепозитории бинарник добавляется в `PATH` корневыми скриптами).
- **Автоподхват компонентов** через `import.meta.glob("/custom/components/*.{tsx,jsx}")`: без генерации файла и слежения за папкой, HMR из коробки.
- **Режимы:** `server` = `ssr: true` + пререндер страниц (проверено: настоящие 302/404 и API поиска в проде); `static` = `ssr: false` + пререндер (проверено: `/` и `/en/docs` через `meta refresh`, поиск в браузере). Список пререндера строит `prerenderPaths()` по файлам `content/docs` (страница × язык, OG, llms, sitemap, robots).
- **Шрифт Geist** через `@fontsource-variable/geist(+mono)` вместо `next/font`. **OG** через `fumadocs-ui/og/takumi` (`takumi-js`), формат PNG.

### Что проверено
- Сборка демо (около 270 пререндеров, ~25 МБ из-за OG-PNG и Mermaid), `bun run check` (Biome, typecheck ядра и двух приложений, 43 теста).
- Внешний проект вне монорепозитория с `bun link`: собирается **без** обходных путей (`.bin/docsivi` появляется, доработка `turbopack.root` больше не нужна).
- В dev: гидратация, счётчик, переключение языка клиентской навигацией (`<html lang>` меняется), SEO-теги в серверном HTML (`title`, `canonical`, `hreflang`, Open Graph), поиск, sitemap, robots, OG, `llms.txt`, 404 со статусом 404.
- Предупреждение `script tag` при переключении языка **исчезло** (корневой layout больше не пересоздаётся при смене языка).

### Находки при переезде
1. **React Router v8:** аргумент `meta` теперь `loaderData`, а не `data` (иначе теги молча не попадают в HTML).
2. `redirect()` в loader при пререндере превращается в страницу с `meta refresh` и задержкой 2 с. Поэтому в серверном режиме `/{lang}/docs` (редирект на версию) не пререндерится, остаётся настоящий HTTP-редирект; в статике рендерим свой `Redirecting` с задержкой 0.
3. В режиме `ssr:false` `loader` разрешён только у пререндеримых маршрутов: для 404 в статике используется маршрут без loader (`not-found-static`), режим передаётся в `routes.ts` через `process.env.DOCSIVI_DEPLOY_MODE`.
4. Макрос Fumadocs вставляет импорт `fumadocs-mdx/runtime/macro` в сгенерированный файл проекта: плагин резолвит такие импорты из ядра, поэтому проекту `fumadocs-mdx` не нужен. Типы сгенерированного `instance.ts` отключены (`@ts-nocheck`), тип `docsivi` для маршрутов задаёт `docsivi:instance`.
5. Vite предупреждает: импорты без расширения в `vite.config.ts` и `docs.config.ts` перестанут работать в будущем мажоре, в проектах пишем `./docs.config.ts`. В `tsconfig.base.json` было `jsx: preserve` (наследие Next), Vite компилировал TSX ядра «как есть», исправлено на `react-jsx`.
6. `createRelativeLink` из Fumadocs асинхронный и серверный, в клиентском рендере не работает: сделан свой синхронный `RelativeLink` на `source.resolveHref`.
7. Bun не создаёт `.bin/docsivi` для `workspace:*`-зависимостей (для `bun link` и установки из реестра создаёт).
8. В dev при первом запуске в консоли шум `504 Outdated Optimize Dep` (оптимизатор зависимостей Vite), на работу не влияет; кандидат на `optimizeDeps.include`.
9. Статический индекс поиска весит около 600 КБ на демо; в серверном режиме `/en/docs` даёт цепочку из трёх редиректов (слэш в конце от статики, затем наш редирект), конечный результат корректный.

### Дальше на этой ветке
- Описать в тасклисте актуальный порядок работ (документация каркаса, размер сборки, `optimizeDeps`, RSC-вариант, публикация `dist`).
- Сборка ядра в JS перед публикацией остаётся актуальной (Vite сам собирает TS из `node_modules` через `ssr.noExternal`, но `react-router.config.ts` и `vite.config.ts` загружаются Node/esbuild).
- 2026-09-24: **OpenAPI (Scalar/Fumadocs).** Решение владельца: отдельная страница «как Changelog», не в дереве документации, на вход только `openapi.json`, «Try it» включён, `fumadocs-openapi` всегда в ядре. Реализовано: `openapi: { input, title }` в `docs.config.ts` (файл, URL или список); маршрут `/:lang/api/*` (страницы операций строятся на лету через `staticSource()`, без файлов и команд, по операции и языку: `/en/api/addJob`, `/ru/api/getStats`); ссылка «API» появляется в верхнем меню сама; `/{lang}/api` ведёт на первую операцию; страницы попадают в пререндер и sitemap; стили `fumadocs-openapi/css/preset.css` в теме. Внешний вид: адрес сервера, метки методов в сайдбаре, параметры, тело запроса, TypeScript-типы, кнопка «Send». Серверный код `fumadocs-openapi/server` подключается динамически внутри `loader`, поэтому в браузерный бандл не попадает (проверено сборкой). В демо добавлен `apps/demo/openapi.json` (HTTP API Lattice: 4 операции, схемы, примеры). Не сделано: прокси для «Try it» при отсутствии CORS у API (`createProxy` у Fumadocs), группировка операций по тегам в сайдбаре, локализация текста самих схем.
- 2026-09-24: **шум оптимизатора Vite исправлен.** На холодном кэше Vite обнаруживал зависимости ядра двумя волнами и перезагружался посреди первой загрузки: гидратация первой страницы ломалась (`Failed to fetch dynamically imported module entry.client.tsx`, 504 Outdated Optimize Dep). Теперь плагин заранее перечисляет их в `optimizeDeps.include` через синтаксис `docsivi > пакет` (проекту эти зависимости не нужны). Проверено: чистый кэш, первая загрузка интерактивна, в логе одно событие оптимизации. Список нужно пополнять при добавлении в ядро новых клиентских зависимостей.
- 2026-09-24: **OpenAPI переведён на настоящий Scalar (замена предыдущей записи выше).** Владелец заметил, что писать свой отрисовщик это велосипед. Проверено на его реальной схеме (`apps/demo/openapi.json`, «Bmstu LK API»: 3 операции, 9 моделей): страница `/{lang}/api` теперь рендерит `@scalar/api-reference-react` (сайдбар с группами по тегам, **раздел Models**, клиентские библиотеки, встроенный API-клиент), внутри нашей верхней навигации, тема сайта (светлая/тёмная) синхронизирована, шрифты сайта. Отключены облачные кнопки Scalar: `showDeveloperTools: "never"`, `agent.disabled`, `mcp.disabled`, `hideDarkModeToggle`. Удалено: собственный рендер моделей, страницы операций на `fumadocs-openapi`, зависимости `fumadocs-openapi`/`@scalar/api-client-react`/`json-schema-typed`. Схема читается в `loader` на сервере (файл) или передаётся как `url`. Пререндерится и попадает в sitemap одна страница на язык. Работает в server и static режимах. Компромиссы Scalar: страница отрисовывается только в браузере (в HTML нет содержимого справочника, для SEO остаётся `title`/canonical, у Fumadocs-варианта были страницы на каждую операцию), тексты схемы не локализуются, свой сайдбар и поиск Scalar рядом с нашими. Если понадобится SEO по операциям, можно вернуть страницы Fumadocs как второй режим. Мелочь: `tsconfig` ядра получил `vite/client` (импорт CSS).
- 2026-09-24: **единая шапка и палитра Scalar.** По просьбе владельца (шапка «как на 1 картинке»: логотип слева, поиск по центру, ссылки, GitHub, язык и тема справа) на **всех** страницах один и тот же компонент: документация использует layout Fumadocs `notebook` с `nav.mode: "top"`, а главная, страница API и 404 используют общий `SiteLayout` (тот же `notebook` без сайдбара: пустое дерево, `--fd-sidebar-width: 0`, сайдбар скрыт). Проверено измерением: координаты логотипа, поиска и ссылок совпадают на главной и на API (`24/16`, `458/10`, `893/1015/1061`), шапка 56 px, поэтому при переходах ничего не «дёргается». Слева от прежнего варианта (`HomeLayout`) отказались. Палитра токенов теперь взята **с живой страницы Scalar**: тёмная `#0f0f0f / #1a1a1a / #272727`, текст `#e7e7e7 / #a4a4a4`, линии `#2d2d2d`; светлая `#fff / #f6f6f6`, текст `#1b1b1b / #757575`, линии `#dfdfdf`; сайдбар в цвет фона; кнопки чёрные в светлой и белые в тёмной теме. Радиус и шрифт Geist не менялись.
- 2026-09-24: **баг исправлен:** пункты `nav` из `docs.config.ts` не получали языковой префикс, поэтому ссылка «Changelog» в шапке вела на `/docs/v2/changelog` (404). Теперь внутренние ссылки проходят через `resolveHref` (`/docs` → версия по умолчанию, `/x` → `/{lang}/x`, внешние и якоря без изменений), функция общая для главной и меню, покрыта тестом. Changelog в демо расширен (5 релизов, бейджи `beta`/`breaking`, компонент `Since`) и переведён на русский.
- 2026-09-24: **сайдбар Scalar в нашем стиле и поиск в шапке (не закоммичено, ждёт проверки владельцем).** Стили в `packages/core/src/theme/scalar.css` (подключаются из `components/scalar-reference.tsx`; чтобы вернуть вид Scalar, достаточно убрать этот импорт). Совпадает с сайдбаром документации, измерено: ширина 268, пункт 236x36, скругление 8, отступы 16, Geist 14 px, hover `accent/50`, активный `primary/10`, шрифт и радиусы Scalar заменены на наши (`--scalar-font`, `--scalar-radius*`), сайдбар прилипает под нашей шапкой (`--refs-header-height: 56px`, раньше было 0). Ширина, отступ и цвета заданы переменными Scalar; высота пунктов, скругление и скрытие поиска сделаны по внутренним классам Scalar (`.t-doc__sidebar`, `.peer\/button`, `button.bg-sidebar-b-search`), версия Scalar закреплена точной (0.9.72), при обновлении эти правила надо перепроверять. **Поиск:** на странице `/{lang}/api` нажатие на поиск в шапке и ⌘K открывают поиск Scalar (программный клик по его скрытой кнопке в сайдбаре), окно документации не показывается; на остальных страницах поиск документации без изменений. Реализовано подменой `SearchDialog` у `RootProvider` (`components/search-bridge.tsx`, отдельные варианты для server и static). Горячая клавиша самого Scalar сменена на `J` (`searchHotKey`), чтобы не дублировать ⌘K. Проверено: клик и ⌘K открывают окно Scalar (`Search...`, результаты по операциям и моделям), на документации открывается обычное окно. Ограничения: если Scalar ещё не отрисовался или в новой версии изменится разметка, мост ничего не откроет (окно закроется); «Open API Client» и «Powered by Scalar» внизу сайдбара остаются.
- 2026-09-24: **исправления страницы API (не закоммичено).** (1) В шапке на странице API появилась кнопка сворачивания сайдбара (как в документации): `SiteLayout` получил `sidebarToggle`, Scalar-сайдбар сворачивается по атрибуту `data-sidebar-collapsed` на контейнере layout. (2) Анимация сворачивания делалась рывками: анимация ширины колонки заставляла тяжёлую страницу Scalar перестраивать текст на каждом кадре. Теперь раскладка меняется один раз, а движение идёт только через `transform`: сайдбар (`transform` + прозрачность, 250 мс, кривая `cubic-bezier(.4,0,.2,1)` как у сайдбара документации, CSS) и контент (FLIP через Web Animations API, `useSlideContentWithSidebar` в `scalar-reference.tsx`). Проверено: анимации создаются с нужными ключевыми кадрами, колонка сетки не анимируется. Кадры глазами проверить не удалось (панель предпросмотра была скрыта и таймлайн стоял), нужна проверка владельцем. `prefers-reduced-motion` отключает анимацию. (3) Мобильная версия: раньше кнопка меню открывала пустое размытое окно, потому что `className: "hidden"` у сайдбара `SiteLayout` применялся и к мобильному выдвижному меню; ссылок Documentation/API/Changelog в нём не было. Теперь меню (`#nd-sidebar-mobile`) открывается и содержит все ссылки на главной, API и 404; на широком экране (от 1024 px) пустая колонка убирается правилами `data-site-layout` в `theme.css`, на планшете (768–1023) остаётся узкий сайдбар со ссылками. На странице API на телефоне есть ещё собственное меню Scalar «Open Menu» (операции и модели), его панель прилипает под нашей шапкой (y=56). (4) Найдена и исправлена ошибка: Scalar не менял тему при переключении темы сайта (читает её один раз); теперь пересоздаётся через `key={resolvedTheme}`, проверено переключением в обе стороны.
- 2026-09-24: **разные цвета фона при быстрой перезагрузке (не закоммичено).** Оттенок «фиолетового» воспроизвести не удалось (все измеренные фоны нейтральные), но найдены и убраны причины расхождения: (1) у `<html>` не было своего фона (виден цвет холста браузера) и `color-scheme`, теперь `html, body { background-color: var(--background) }` и `color-scheme` по теме; (2) класс темы ставил скрипт `next-themes` внутри `<body>`, чуть позже первой отрисовки: добавлен свой короткий скрипт в `<head>` (`earlyTheme` в `react-router/root.tsx`, тот же ключ `theme` в localStorage и логика `system`), плюс `<meta name="color-scheme">`; (3) цвета Scalar теперь заданы через наши токены (`--scalar-background-1: var(--background)`, `--scalar-background-2: var(--card)`, `--scalar-color-1/2`, `--scalar-border-color`), поэтому расходиться с сайтом они не могут, в том числе при собственной теме. Проверено измерением после перезагрузки: `html`, `body`, шапка, `.scalar-app` и его сайдбар одного цвета (тёмная `rgb(15,15,15)`, светлая `rgb(255,255,255)`), класс `dark` есть уже в первом замере. Если тот самый фиолетовый оттенок останется, нужен скриншот или название браузера/режима (dev или сборка), чтобы найти конкретный источник.
- 2026-09-24: **«Powered by Scalar» удалён из кода, а не спрятан (не закоммичено).** Плагин `docsivi:no-scalar-credit` (`packages/core/src/vite/scalar-credit.ts`) вырезает подпись при сборке и в dev-предсборке зависимостей (`optimizeDeps.rolldownOptions.plugins`). Ссылка была в двух местах, убирать нужно оба: слот `description` в `@scalar/api-reference` (`ApiReference.vue.script.js`) и запасное содержимое в `@scalar/components` (`ScalarSidebarFooter.vue.script.js`; при пустом слоте Vue берёт его). Проверено в dev (`poweredByInDom: false`, в футере остаётся только кнопка «Open API Client») и в production-сборке. Лицензия Scalar MIT, обязательной подписи в интерфейсе не требует. Код ищется по форме и рассчитан на закреплённую версию (0.9.72); если после обновления Scalar форма изменится, при сборке будет предупреждение, а подпись останется скрытой правилом в `scalar.css` (оно оставлено как запасное). Тесты на реальных фрагментах кода Scalar: `test/scalar-credit.test.ts`. Словарь переводов Scalar (`footer.poweredByScalar`) остаётся в бандле, он нигде не отображается.

### Блог (2026-09-24)
- Включается блоком `blog` в `docs.config.ts` (категории, авторы, `perPage`, `share`, `rss`). Ссылка «Blog» появляется в верхней навигации сама; ручной Changelog в навигации демо убран, короткий changelog остаётся в доках.
- Посты: `content/blog/<slug>[.<lang>].mdx`, перевод с откатом на язык по умолчанию. Заголовок проверяется Zod, неизвестные категории и авторы останавливают сборку.
- **Без утечек:** черновики и посты с будущей датой не попадают в список `files` сгенерированной коллекции `.docsivi/blog.ts`, поэтому не компилируются, не пререндерятся и не видны в сборке (проверено поиском маркеров по `build/`). `DOCSIVI_DRAFTS=1` показывает всё при работе над текстом.
- Маршруты: `/:lang/blog` (список: категории, поиск по названию/тегам/авторам, Filter с тегами, 12 на страницу), `/:lang/blog/:slug` (обложка, автор, оглавление сверху, поделиться, «Читайте также»), `rss.xml`, `og.png` (обложка поста как фон, градиент без неё). Sitemap и пререндер учитывают только опубликованные посты.
- MDX-компоненты ядра: Authors, Expand, PR, Benchmark, Figure, Embed, CTA.
- Демо: 5 постов (один с переводом), черновик и отложенный пост для проверки утечек. Тесты: 73 проходят.
- Не сделано: docs-страница «как писать посты», проверка статичного режима в браузере, реальные PNG-обложки (в демо SVG).

### Реорганизация папок ядра (2026-09-24)
`packages/core/src` разложен по фичам вместо слоёв:
- `features/<имя>/` (docs, blog, api-reference, search, home, seo, site): маршруты, данные, UI, стили и при необходимости пререндер и sitemap одной фичи лежат вместе. Удалить фичу = удалить папку и строку в списке.
- `features/types.ts`, `features/index.ts`: дескриптор фичи `{id, dir, enabled, routes, nav}`. Список маршрутов, ссылок верхней навигации, пререндера (`features/prerender.ts`) и sitemap (`features/sitemap.ts`) собирается из фич, а не перечисляет их по имени.
- `shared/`: общее для всех фич (layout, ui, links, messages, versions, instance, router-хелперы).
- `build/router`, `build/vite`: всё, что работает при сборке (scaffold, routes, prerender, plugin, root).
- Остались на месте: `config/`, `plugins/`, `mdx/`, `theme/`, `index.ts`.
- Публичные имена в `exports` пакета не менялись, только пути внутри. Формат проекта пользователя тот же.
- Как добавить фичу: папка с `feature.ts`, строка в `features/index.ts` (и в `prerender.ts` / `sitemap.ts`, если есть что пререндерить или добавить в sitemap).
- Проверено: `bun run check` (73 теста), сборка демо (80 html) и стартера, dev.
