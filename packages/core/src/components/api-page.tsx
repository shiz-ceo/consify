"use client";

import { createOpenAPIPage } from "fumadocs-openapi/ui";

/** Renders one operation of the API reference: parameters, schemas, examples and the playground. */
export const OpenAPIPage = createOpenAPIPage();
